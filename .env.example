# LabLLM - No environment variables required!
# API keys are entered per-session in the browser.
# This file exists for documentation purposes only.

# If you want to pre-configure anything in the future,
# add VITE_ prefixed variables here.
